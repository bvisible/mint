# //// Neoffice — NOT ours: the version string tracks upstream. We hand-carried upstream
# //// v1.5.0 then the v1.5.2 fixes (89e7929, 2bdac9c) instead of merging their branch, so
# //// this line moved on its own. At the next merge take upstream's and drop this marker.
__version__ = "1.5.2"
