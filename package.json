{
  "name": "mint",
  "version": "1.0.0",
  "description": "Bank reconciliation made simple for ERPNext",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && yarn install",
    "dev": "cd frontend && yarn dev",
    "build": "if [ -d 'mint/public/mint/assets' ] && [ -z \"$FORCE_REBUILD\" ]; then echo '[skip-build] pre-built artifacts present at mint/public/mint/assets (set FORCE_REBUILD=1 to force rebuild)'; else yarn build:force; fi",
    "build:force": "cd frontend && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}